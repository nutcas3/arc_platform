<div class="bg-gray-50 border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
        <div class="flex justify-end">
            <%= form_with url: projects_path, method: :get, class: "relative w-64" do |f| %>
                <%= f.text_field :query, value: params[:query], placeholder: "Search here...", class: "w-full px-4 py-2 border border-gray-300 rounded text-sm focus:outline-none focus:ring-2 focus:ring-red-600" %>
                <button type="submit" class="absolute right-2 top-2.5 text-gray-400">🔍</button>
            <% end %>
        </div>
    </div>
</div>
